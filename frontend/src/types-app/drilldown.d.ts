// Types de static/modules/drilldown.js (application existante, JavaScript).
export declare function drilldownPositions(positions: unknown[], titre: string, sousTitre: string,
  options?: { showOwner?: boolean; valueField?: string; neg?: boolean }): void;
